import 'package:flutter/material.dart';

class AddPalaceScreen extends StatelessWidget {
  const AddPalaceScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.lightGreen,
    );
  }
}
