import 'package:flutter/material.dart';

class LibraryItemCardWidget extends StatelessWidget {
  const LibraryItemCardWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
